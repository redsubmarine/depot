<div id="<%= dom_id cart %>" class="my-5 space-y-5">
  <h2 class="font-bold text-lg mb-3">Your Pragmatic Cart</h2>

  <ul class="list-disc list-inside">
    <% cart.line_items.each do |item| %>
      <li><%= item.product.title %></li>
    <% end %>
  </ul>
</div>
